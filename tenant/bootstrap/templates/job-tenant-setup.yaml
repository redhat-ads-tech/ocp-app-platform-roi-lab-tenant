apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "tenant-bootstrap.name" . }}-setup
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  backoffLimit: 100
  template:
    spec:
      serviceAccountName: {{ include "tenant-bootstrap.name" . }}
      containers:
        - name: tenant-setup
          command:
            - /bin/bash
            - '-c'
            - |
              set -x
              ansible-playbook -i localhost /scripts/playbook.yaml
          image: quay.io/agnosticd/ee-multicloud:latest
          volumeMounts:
            - mountPath: /scripts
              name: scripts
      restartPolicy: Never
      volumes:
        - name: scripts
          configMap:
            name: {{ include "tenant-bootstrap.name" . }}-setup
